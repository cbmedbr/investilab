import { NextResponse } from 'next/server'

// Placeholder — será implementado na Fase 2
export async function GET() {
  return NextResponse.json({
    message: 'API de indicadores será implementada na Fase 2.',
    indicators: [],
  })
}
